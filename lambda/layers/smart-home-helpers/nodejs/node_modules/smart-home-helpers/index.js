const AWS = require("aws-sdk");
const dynamodb = new AWS.DynamoDB.DocumentClient();

module.exports = class Helper {
    constructor() {
      this.applicationTable = '';  // the DynamoDB user table containing our Cognito user to IoT Thing mappings
    }
    set userTable(applicationTable) {
      this._applicationTable = applicationTable;
    }
    get applicationTable() {
      return this.applicationTable;
    }
    async getUserEndpoints(userId) {
        return `${userId} - thing 123`;
    }
  }